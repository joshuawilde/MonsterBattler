namespace MonsterBattler.Sim.Effects.Abilities
{
    /// <summary>Cud Chew: would re-eat a Berry — the Berry item system is not modeled, so this is inert.</summary>
    public sealed class CudchewEffect : Effect
    {
        public override string EffectId => "cudchew";
        public override string DisplayName => "Cudchew";
    }
}
